import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.umar.plugin',
  appName: 'umarPlufin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
